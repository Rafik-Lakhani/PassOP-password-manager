<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password storage tool | LR</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main bg-green-100 min-h-screen w-full flex flex-col justify-center items-center">
        <div class="flex justify-between p-4 px-8 w-full bg-slate-800 text-white">
            <div class="left text-2xl font-bold">
                <h1><span class="text-green-700 ">
                        <</span>Pass<span class="text-green-700">OP/></span></h1>
            </div>
            <h1 class=""><a href="/">Home</a></h1>
        </div>
        <div class="flex flex-col p-6 items-center justify-center w-[470px] m-auto gap-6 border-2 border-green-500 rounded-[25px]">
               <h1 class="text-3xl font-bold text-green-800">OTP Verification</h1>
               <div>
                   <h5>Enter OTP is send your email <%=email%></h5>
                    <h5 id="timer">00:60</h5>
               </div>

               <%   if(from!="login"){%>
                  <form action= "/verifyemail?em=<%=email%>" class="w-full flex flex-col items-center gap-2" method="post"> 
                <% }else{ %>
                    <form action="/loginemailverify?email=<%=email%>" class="w-full flex flex-col items-center gap-2" method="post">
                 <% } %>   
                <input type="number" name="otp" placeholder="Enter OTP" class="w-4/5 px-2 py-1 border-2 border-green-500 rounded-[20px]" maxlength="6" required>
                <input type="submit" value="Submit"
                class=" bg-green-500 w-[80px] h-10 mt-[20px] text-black text-center m-auto  text-green-800 [16px] rounded-full border-2 border-zinc-900 hover:bg-green-400">
               </form>
               
        </div>
    </div>

    <script>
    let i = 60;
    const intervalId = setInterval(() => {
        i--;
        if (i == 0) {
            clearInterval(intervalId);
            document.getElementById('timer').innerHTML='<a href="/tryagain" class="text-blue-900"> Try Again</a>';
        }
        else{
            document.getElementById('timer').innerText =`00:${i}`;
        }
    }, 1000);
</script>

</body>
</html>