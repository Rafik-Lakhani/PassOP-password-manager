<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Password storage tool | LR</title>
    <link
      href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css"
      rel="stylesheet"
    />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="main bg-green-100 min-h-screen w-full flex flex-col justify-center items-center"
    >
      <div class="flex justify-between p-4 px-8 w-full bg-slate-800 text-white">
        <div class="left text-2xl font-bold">
          <h1>
            <span class="text-green-700"> <</span>Pass<span
              class="text-green-700"
              >OP/></span
            >
          </h1>
        </div>
        <h1 class=""><a href="/">Home</a></h1>
      </div>
      <div
        class="flex flex-col p-6 items-center justify-center [400px] w-[400px] m-auto gap-2 border-2 border-green-500 rounded-[25px]"
      >
        <h1 class="text-3xl font-bold text-green-800 mb-6">Login</h1>
        <% if(message){ %>
        <h1 class="bg-red-300 text-white"><%= message %></h1>
        <% } %>
        <form
          action="/loginuser"
          class="w-full flex flex-col items-center gap-2"
          method="post"
        >
          <input
            type="text"
            name="email"
            placeholder="Enter Email"
            class="w-4/5 px-2 py-1 border-2 border-green-500 rounded-[20px]"
          />
          <input
            type="text"
            name="password"
            placeholder="Enter Password"
            class="w-4/5 px-2 py-1 border-2 border-green-500 rounded-[20px]"
          />
          <input
            type="submit"
            value="Verify"
            class="bg-green-500 w-[70px] h-8 mt-[20px] text-center m-auto text-[18px] rounded-full border-2 border-zinc-900"
          />
        </form>
        <h3 class="text-green-800">
          Don't Have Account?
          <a href="/register" class="text-black">Register</a>
        </h3>
      </div>
    </div>
  </body>
</html>
