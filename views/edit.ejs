<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Password</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.3.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>
    <body class="main bg-green-100 min-h-screen w-full flex flex-col items-center">
        <div class="flex justify-between p-4 px-8 w-full bg-slate-800 text-white">
            <div class="left text-2xl font-bold">
            <h1>
                <span class="text-green-700"> <</span>Pass<span
                class="text-green-700"
                >OP/></span
                >
            </h1>
            </div>
            <h1 class=""><a href="/">Home</a></h1>
        </div>
        <div class="flex flex-col p-6 items-center justify-center [400px] w-[400px] m-auto gap-2 border-2 border-green-500 rounded-[25px]">
            <h1 class="text-3xl font-bold text-green-800 mb-6">Edit Password</h1>
            <form action="/edit/<%= password._id %>" method="POST" class="w-full flex flex-col items-center gap-2">
                <div class="relative w-[90%]">
                    <label for="siteurl" class="block text-green-800 mb-2">Site URL:</label>
                    <div class="flex items-center">
                        <i class="ri-global-line absolute left-3 text-gray-400"></i>
                        <input type="text" id="siteurl" name="siteurl" required
                        class=" px-2 py-1 pl-10 border-2 border-green-500 rounded-[20px] w-full"
                        value="<%= password.siteurl %>">
                </div>
            </div>
            <div class="relative w-[90%]">
                <label for="username" class="block text-gray-700 mb-2">Username:</label>
                <div class="flex items-center">
                    <i class="ri-user-line absolute left-3 text-gray-400"></i>
                    <input type="text" id="username" name="sideusername" required
                        class=" px-2 py-1 pl-10 border-2 border-green-500 rounded-[20px] w-full"
                        value="<%= password.usernm %>">
                    </div>
                </div>
            <div class="relative w-[90%]">
                <label for="password" class="block text-gray-700 mb-2">Password:</label>
                <div class="flex items-center">
                    <i class="ri-lock-line absolute left-3 text-gray-400"></i>
                    <input type="text" id="password" name="sitepwd" required
                    class=" px-2 py-1 pl-10 border-2 border-green-500 rounded-[20px] w-full"
                    value="<%= password.sitepw %>">
                </div>
            </div>
            <button type="submit" 
                class="bg-green-500 py-1 px-7 h-8 mt-[20px] text-center m-auto text-[18px] rounded-full border-2 border-zinc-900">
                Update
            </button>
            </form>
        </div>
    </div>
</body>
</html>